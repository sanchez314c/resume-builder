# Resume Builder Environment Variables

# Electron sandbox (set to 1 to disable on Linux if needed)
ELECTRON_NO_SANDBOX=0

# GPU acceleration (set to 1 to disable)
DISABLE_GPU=0

# Vite dev server URL (auto-detected, override if needed)
# VITE_DEV_SERVER_URL=http://localhost:5173
